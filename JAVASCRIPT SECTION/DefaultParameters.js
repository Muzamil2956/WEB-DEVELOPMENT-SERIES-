function sum ( a, b=2) {
    return a+b;
}
result = sum(3); // 5  here 3 is value of of a and if a = something in default then it is wrong or default parameter should be used at end 
console.log(result);