// We have to write a code for good string that has length greater than 3 and starts with letter a

let str = "app";
if ( str.length > 3 && str[0] === "a") {
    console.log( "String is Good");
}
else {
    console.log("String is not good");
}